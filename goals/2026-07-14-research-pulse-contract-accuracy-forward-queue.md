# Forward queue after Research Pulse contract accuracy, 2026-07-14

Candidate follow-up surfaced during this work. This is a queue, not a roadmap.

- **Client-first rollout** (priority: high, confidence: verified constraint)  
  Release the compatible ResearchBar decoder and menu before the additive server contract to prevent old clients from displaying the unlimited compatibility value as zero credits.

- **Retire fallback aliases after uptake** (priority: medium, confidence: verified debt)  
  Remove legacy balance and tracked-work fallback decoding only after supported servers and installed clients have crossed the documented compatibility window.

- **Exercise the first live 7-day transition** (priority: medium, confidence: verified operational need)  
  Verify a real user moves from `tracking` to `tracked` with a 7-day row and sparkline while the 52-week row remains absent, then verify the annual row appears when sufficient history exists.

- **Investigate the isolated transient test-shard failure if it recurs** (priority: low, confidence: transient)  
  The complete rerun passed. Capture the exact test and environment only if a future full run reproduces it.
