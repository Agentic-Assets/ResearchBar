# CodexBar upstream synchronization - 2026-08-05

## Provenance

- Fork base: `05545feba362ba7626e92d24bf643650a7dff740`
- Merged upstream: `4cdb349cbc57780e524d3e874ef582078b9bf7c2`
- Upstream remote: `https://github.com/steipete/CodexBar.git`

## Resolution

The merge takes the upstream provider-instance, settings, menu, packaging, and test-suite
updates while retaining ResearchBar's app identity, Corbis experience, research status surface,
and product documentation. The resolved integration keeps ResearchBar-specific code under
`Sources/CodexBar/ResearchBar/`, `Sources/CodexBarCore/ResearchBar/`, and
`Tests/CodexBarTests/ResearchBar/`.

The following durable safeguards were added for the next update:

- `ResearchBar/branding/identity.env` is the one packaging identity manifest.
- `Scripts/check_researchbar_identity.sh` guards the product name, bundle IDs, helpers, and UTI.
- `Scripts/sync_codexbar.sh --plan` checks remote provenance and shows the next safe merge plan;
  `--start` creates an isolated worktree and begins a no-commit merge only.
- `ResearchBar/upstream-sync.toml` records the last merged upstream SHA.
- `docs/UPSTREAM_SYNC.md` describes resolution ownership and the verification workflow.

## Verification

- PASS: `swift test list` compiled all current source and test targets.
- PASS: `make check` completed with all repository, locale, package, documentation, format, and
  SwiftLint checks clean.
- PASS: `swift build -c release` completed.
- DEFERRED: `make test` reproduced two `AdaptiveRefreshTimerTests` polling timeouts on this host.
  Both tests and the timer implementation are byte-identical to upstream `4cdb349c`; upstream
  macOS CI passed them. An unrelated installed CodexBar process was consuming about 55-71 percent
  CPU while the tests ran, starving the tested utility-priority detached task. No production or
  upstream test code was changed to mask that host-load artifact.
- BLOCKED: the local ad-hoc package reached the Xcode widget-extension build, then Xcode blocked
  while attempting to obtain Keychain-backed HTTP credentials to download Sparkle's public binary
  artifact. The build was stopped without waiting for or accepting a Keychain prompt. No fresh app
  bundle was installed or launched.

## Release boundary

This is a local merge and release-build validation only. It has not been pushed, submitted as a
pull request, or merged to `main`.
